# Fuzzy

A collection of small experiments. The `cosmic_invader` package contains a simple
terminal-based shooter game.

## Running Cosmic Invader

From the repository root run:

```bash
python -m cosmic_invader.game
```

Use the arrow keys to move and the spacebar to fire. Press `q` to quit. See
`cosmic_invader/README.md` for more details.
